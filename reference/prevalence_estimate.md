# Extract prevalence estimates from malariasimulation output

Extract prevalence estimates from malariasimulation output

## Usage

``` r
prevalence_estimate(x, diagnostic)
```

## Arguments

- x:

  Input data.frame

- diagnostic:

  Can be light microscopy ("lm"), or PCR ("pcr")
