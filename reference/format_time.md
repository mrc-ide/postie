# Adds time columns

Adds year (assuming no leap years), month, week (assuming last week in
the year is 8 days), day and time columns. Time is usuful for plotting.

## Usage

``` r
format_time(x, baseline_year)
```

## Arguments

- x:

  Input data.frame

- baseline_year:

  baseline_year Baseline year
