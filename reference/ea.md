# Expected age within range assuming exponentially distributed pop

Expected age within range assuming exponentially distributed pop

## Usage

``` r
ea(age_lower, age_upper, average_age)
```

## Arguments

- age_lower:

  Lower bound of age range (single value)

- age_upper:

  Upper bound of age range (single value)

- average_age:

  Average age of population
